services:
  backend:
    build:
      context: 
        ./backend
    env_file:
      ./backend/.env
    environment:
      AWS_REGION: eu-central-1
      DYNAMODB_JOBS_TABLE: Jobs
      SCRAPER_USER_AGENT: JobFinderBot/0.1
    ports:
      - "8000:8000"
